[package]
name = "hermit-builtins"
edition = "2024"

[dependencies]
libm = "0.2"

[lib]
crate-type = ["staticlib"]
harness = false

[profile.dist]
inherits = "release"
lto = "thin"
codegen-units = 1

[profile.profiling]
inherits = "dist"
debug = "line-tables-only"

[workspace]
