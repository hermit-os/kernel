[package]
name = "hermit-builtins"
edition = "2024"

[dependencies]
libm = "0.2"

[lib]
crate-type = ["staticlib"]
harness = false

[profile.profiling]
inherits = "release"
debug = "line-tables-only"

[workspace]
